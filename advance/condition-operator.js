const user = 27
let message = user >=18 ? 'you can enter' : 'you can not enter'


console.log(message)
//conditional opperator 


const team =['felix','mark','will','fred','annie']

let alert = team.length <= 4 ? `Team size ${team.length} join now!` : `Too many people on the team`

console.log(alert)


// trufy or falsy 
// thurfy - calues that resolve to true in a boolean context
// falsy - calues that resolve to flase in a boolean context
//falsy values - false,0,empty string, null, undefined 